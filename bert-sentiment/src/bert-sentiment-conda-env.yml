name: bert-sentiment
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.8.13
  - pip=22.0.4
  - pip:
    -hydra-core
    -matplotlib
    -pandas
    -numpy
    -scipy
    -seaborn
    -pillow
    -scikit-image
    -torch
    -torchvision
    -transformers
    -pytest
    -mlflow